import './loader.css'

const Loader = (props) => {
    return (
        <div id='loader-container'>
            <div id='loader-div' style={{height: props.elHeight}}>

            </div>
        </div>
    )
}

export default Loader